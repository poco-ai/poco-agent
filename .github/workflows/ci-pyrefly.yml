name: CI Pyrefly

on:
  pull_request:
    paths:
      - "backend/**"
      - "executor/**"
      - "executor_manager/**"
      - "pyproject.toml"
      - ".pre-commit-config.yaml"
      - ".github/workflows/ci-pyrefly.yml"
  push:
    branches:
      - main
    paths:
      - "backend/**"
      - "executor/**"
      - "executor_manager/**"
      - "pyproject.toml"
      - ".pre-commit-config.yaml"
      - ".github/workflows/ci-pyrefly.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pyrefly:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        service:
          - backend
          - executor
          - executor_manager
    name: Pyrefly (${{ matrix.service }})
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Run Pyrefly
        working-directory: ${{ matrix.service }}
        run: uv run --frozen --with pyrefly==0.47.0 pyrefly check
